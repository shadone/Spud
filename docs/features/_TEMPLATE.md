# <Capability name>

- **Surfaces:** <comma-separated tags from the legend in README.md>
- **Status:** shipped            <!-- shipped | partial — <what's missing> -->
- **Related:** <clickable links to the plan / design doc / sibling feature doc that produced this behavior — NOT source code, e.g. [mark-read-and-hiding.md](mark-read-and-hiding.md)>

<!-- These are product-feature descriptions for an end-user audience, with a
     sprinkle of technical detail — not implementation docs. Do NOT link to
     source files (.swift) anywhere in the doc; reference symbols as inline code
     (`HideReadPostsFilter`), never as links. Source links rot as code moves and
     couple the doc to implementation that can change without the product
     behavior changing. Link only plans / design docs / sibling feature docs. -->

## What it does

2-4 sentence prose summary. Skimmable. The "what does Spud support" answer.
Present tense, observable behavior only — no history, no rationale.

## Behavior and rules

- Bulleted invariants and decisions that hold across all scenarios below.
- Example: "A short swipe fires the primary action for its direction; a deeper swipe fires the secondary."

## Scenarios

### <Scenario title>

- **Surfaces:** <subset — include ONLY if narrower than the doc-level Surfaces line>
- **Given** <precondition>
- **When** <user action>
- **Then** <observable outcome>
- **And** <additional outcome / edge note, optional>

## Not supported / out of scope

- Explicit non-behaviors so neither a human nor an agent assumes them.
